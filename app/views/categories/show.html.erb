<div class="row">
    <div class="col-md-3">
      <%= render 'categories/sidebar' %>
    </div>

    <div class="col-md-9">
        <div class="row">
          <% @category.services.published.each do |service| %>
              <div class="col-sm-4 col-lg-4 col-md-4">
                  <div class="thumbnail">
                  
                      <img src="<%= service.photo %>" alt="" class="img-search">
                      <div class="caption">
                          <h4><%= link_to truncate(service.name, length: 19), service_path(service) %></h4>

                      </div>
                  </div>
              </div>
          <% end %>
        
            <div class="col-sm-4 col-lg-4 col-md-4">
                <h4><%= link_to '¿Quieres subir tu servicios?', new_service_path%></a></h4>
                <p>Anunciarte en servicios en un click es muy facil, solo tienes que hacer click en el boton de abajo</p>
                <%= link_to 'Subir Anuncio', new_service_path, class: 'btn btn-primary'%>
            </div>
        </div>
    </div>
</div>
